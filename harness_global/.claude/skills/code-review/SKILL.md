---
name: code-review
description: "기획 의도와 적용 코드를 리뷰할 때 사용하라. '리뷰해봐', '기획 의도랑 어긋난 거 있어?', '코드 검토해줘', '내 기획이랑 맞는지 봐줘' 등 사용자가 이미 구현한 코드를 검토 요청할 때 반드시 이 스킬을 사용하라."
---

# 기획-코드 리뷰어

사용자가 제시한 기획 의도와 실제 적용 코드를 4단계로 검토한다.

## 입력 조건

이 스킬은 두 가지를 입력으로 받는다:
- **기획 의도**: 사용자가 텍스트로 설명한 기능의 목적, 동작 방식, 엣지케이스
- **코드 범위**: 브랜치명, 파일 경로, 컴포넌트 명 중 하나 이상

둘 중 하나가 없으면 사용자에게 먼저 요청한다.

---

## Phase 1: 기획 의도 파악

사용자가 설명한 내용에서 아래를 추출한다:

- **핵심 동작**: 사용자가 만들려는 것이 무엇인가 (한 문장)
- **데이터 흐름**: 입력 → 처리 → 출력의 흐름
- **엣지케이스 목록**: 사용자가 명시한 예외 상황들 (번호 매겨 목록화)
- **불명확한 점**: 기획 설명에서 해석이 두 가지 이상 가능한 부분

불명확한 점이 있으면 리뷰 결과에 "기획 모호 지점"으로 표시하되, 리뷰 진행은 멈추지 않는다.

---

## Phase 2: 코드 절충 검토

관련 파일을 직접 읽어 기획 의도와 코드를 1:1 대응한다.

**탐색 순서:**
1. 사용자가 언급한 파일/컴포넌트 직접 읽기
2. 해당 파일을 import하거나 사용하는 상위 파일 탐색 (Grep)
3. emit/event 핸들러 연결 확인

**체크 항목:**
- 기획의 핵심 동작이 코드로 구현되어 있는가?
- 각 엣지케이스에 대응하는 코드 분기가 있는가? (있으면 라인 번호 명시)
- 기획에는 있는데 코드에 없는 항목이 있는가?
- 코드에는 있는데 기획에 없는 항목이 있는가? (과잉 구현 또는 기획 누락 가능성)

**출력 형식:**
```
## 코드 절충 검토

### 일치
- [기획 항목] → `파일:라인` 구현 확인

### 불일치
- [기획 항목] — 코드 없음 / 코드가 다르게 동작함
  - 기획: ...
  - 코드: `파일:라인` ...

### 코드에만 존재
- `파일:라인` — 기획에 없는 로직 (의도 확인 필요)
```

---

## Phase 3: 연결된 도메인 스캔

이 기능이 직접 참조하거나 참조받는 도메인을 나열한다.

**스캔 범위:**
- 현재 파일이 import하는 스토어/서비스/컴포넌트
- 현재 파일을 import하거나 이벤트를 구독하는 상위 파일
- 같은 스토어/서비스를 사용하는 다른 파일

**출력 규칙:**
- 도메인명과 파일 경로만 나열한다
- "영향을 끼칠 수 있다/없다"는 판단하지 않는다
- 영향 판단은 사용자가 한다

**출력 형식:**
```
## 연결된 도메인

| 도메인 | 파일 | 연결 방식 |
|--------|------|----------|
| 검수 페이지 | `src/pages/inspection/PageInspection.vue` | DialogMyVoice를 직접 호출 |
| 타임라인 믹스인 | `src/components/mixins/ToonWorkMixin.vue` | _sortedSpoints computed 공유 |
| ... | ... | ... |
```

---

## Phase 4: 잠재적 에러 진단

코드를 읽으며 발견한 잠재적 문제를 진단한다.

**진단 항목:**
- 런타임 에러 가능성 (undefined 접근, 배열 경계, 비동기 처리 누락)
- 기획 엣지케이스의 조건 로직이 의도와 다르게 동작하는 경우
- 이벤트/emit 핸들러가 연결되어 있지 않은 경우
- 상태가 초기화되지 않거나 정리되지 않는 경우

**심각도 기준:**
- `[치명]` — 기능이 동작하지 않거나 데이터 손실 위험
- `[주의]` — 특정 조건에서만 발생하는 오동작
- `[확인]` — 의도를 알 수 없어 사용자 확인이 필요한 코드

**출력 형식:**
```
## 잠재적 에러

- [치명] `파일:라인` — 에러 상황 설명
  - 발생 조건: ...
  - 현재 코드: (코드 스니펫)

- [주의] `파일:라인` — ...

- [확인] `파일:라인` — ...
```

---

## 최종 출력 구조

```
## 기획 의도 파악
[핵심 동작 한 줄]
[데이터 흐름]
[엣지케이스 목록]
[기획 모호 지점 — 있을 경우만]

---

## 코드 절충 검토
[일치 / 불일치 / 코드에만 존재]

---

## 연결된 도메인
[테이블]

---

## 잠재적 에러
[치명 / 주의 / 확인]
```

---

## 작업 원칙

1. **직접 읽기** — 파일을 실제로 열어 라인 번호를 확인한다. 추측하지 않는다.
2. **판단 분리** — Phase 3은 나열만 한다. 영향 판단은 사용자 몫이다.
3. **기획 우선** — 기획 의도가 기준이다. 코드가 기획과 다르면 코드가 틀린 것으로 본다.
4. **라인 번호 필수** — 모든 코드 언급에 `파일:라인` 형식을 포함한다.
5. **멈추지 않기** — 불명확한 기획이 있어도 알 수 있는 부분까지 진행하고 모호한 부분만 표시한다.
